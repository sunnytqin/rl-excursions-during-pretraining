<script>import MarkdownTokens from "../../MarkdownTokens.svelte";
export let token;
export let options;
export let renderers;
</script>

<table>
  <thead>
    <tr>
      {#each token.header as item}
        <th scope="col">
          <MarkdownTokens tokens={item.tokens} {options} {renderers} />
        </th>
      {/each}
    </tr>
  </thead>
  <tbody>
    {#each token.rows as row}
      <tr>
        {#each row as col}
          <td>
            <MarkdownTokens tokens={col.tokens} {options} {renderers} />
          </td>
        {/each}
      </tr>
    {/each}
  </tbody>
</table>
