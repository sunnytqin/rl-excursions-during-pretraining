<script>import { generatePathSegment } from "../../utils/url";
export let token;
export let options;
export const renderers = void 0;
let id;
$: id = generatePathSegment(token.text, options.slugger);
</script>

<svelte:element this={`h${token.depth}`} {id}>
  <slot />
</svelte:element>
